/**
 * Created by Anonymous11100001 on 5/4/2016.
 */
package access.foreign;
import access.local.*;

public class ForeignClass {
    public static void main(String[] args){
        //PackagedClass obj = new PackagedClass();
        //Foreign class can not access to Packaged class because packaged class has
        // Package access (but not public access). So that foreign class is out of
        // package (directory ) with packaged clasd, it cpuldn't access to packaged class
    }
}
